# Engineering Rules & Conventions

➡️ The canonical rules live at the repository root: [`/rules.md`](../rules.md).

This pointer exists so the numbered `docs/` suite stays complete (01–08).
Update `/rules.md` — not this file.
